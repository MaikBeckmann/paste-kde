paste-kde
---------

Emacs library to post text to KDE's pastebin service.

Usage
-----

Just add the directory to your `load-path' and put inside your
`~/.emacs':

  (require 'paste-kde)

To post a buffer: M-x paste-kde-buffer

To post a region: M-x paste-kde-region
